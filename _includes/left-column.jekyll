<nav>
  <div class="nav">
    <div class="marquee">
      <div class="marquee-text">⚠️⚠️このサイトは立ち上げ中です⚠️⚠️</div>
    </div>
    {% for item in site.data.navigation %}
      <p>
        <a
          href="{{ item.link }}"
          {% if page.url == item.link %}
          style="font-weight: 700;"
          {% endif %}>
          {{ item.name }}
        </a>
      </p>
    {% endfor %}
  </div>
</nav>